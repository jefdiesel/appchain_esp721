{
  "name": "chainhost-workers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:email": "wrangler deploy -c wrangler-email-receiver.toml",
    "db:migrate": "wrangler d1 execute chainhost-email --file=../sql/email-schema.sql",
    "db:migrate:local": "wrangler d1 execute chainhost-email --local --file=../sql/email-schema.sql"
  },
  "dependencies": {
    "postal-mime": "^2.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
